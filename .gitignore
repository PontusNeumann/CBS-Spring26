ML/assignments/ML MA3/data/

# macOS
.DS_Store

# Polymarket data — heavy binaries kept out of git (distributed via GitHub Releases).
# README.md / MISSING_DATA.md / release-manifest*.md / *.info.json / .gitkeep stay tracked.
ML/report/data/**/*.parquet
ML/report/data/**/*.csv
ML/report/data/**/*.log

# Python bytecode
__pycache__/

# Secrets — never commit
.env
ML/report/.env

# Per-person scratch caches (alex/data/ and pontus/data/ no longer exist post-2026-04-29 consolidation)
ML/report/alex/.scratch/
ML/report/alex/outputs/.DS_Store
ML/report/alex/outputs/**/.DS_Store

# Claude Code project state
ML/report/.claude/

# Release tarballs downloaded from GitHub Releases (too large for git)
*.tar.gz
