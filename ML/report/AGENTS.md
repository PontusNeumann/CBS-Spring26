# Codex Instructions

Before editing anything in this `report/` folder, read [`CLAUDE.md`](./CLAUDE.md). It is the shared operating brief for Claude and Codex and contains the source-of-truth rules: which files to trust, the live `.docx` filename, the page-1/2 freeze, formatting limits, the conda env, US spelling, and the current pipeline status (Alex is running the pipeline; `submission/outputs/` will populate from his machine).

Same instructions apply to any agent working in this folder.
