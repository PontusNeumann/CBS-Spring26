Codex refactored the v2 analysis into `onestream_pipeline_v3.ipynb`, added the stand-alone `onestream_spot_check.ipynb`, moved the historical notebook to `notebooks/onestream_architecture_comparison_v2.ipynb`, and added `scripts/refine_md_kb.py` for deterministic md-to-md KB refinement. The smoke test used `/tmp/synth_kb/` with four markdown documents across two KB folders and `/tmp/synth_tickets.csv` with 30 synthetic routing tickets plus 5 access change-request rows; both notebooks were executed end to end, with the spot-check run once as `before` and once as `after` to exercise the comparison cells. Linus run order: first run `python3 scripts/refine_md_kb.py --input "<old markdown KB>" --output "<refined markdown KB>" --clear-output`, then inspect `<refined markdown KB>/_refinement_reports/kb_refinement_flags.csv` and manually fix any flagged files that need content judgment, then set `USE_SYNTHETIC_SMOKE_TEST = False`, `TICKETS_CSV`, `KB_FOLDER_OLD`, and `KB_FOLDER_NEW` in `onestream_pipeline_v3.ipynb`, then run that notebook top to bottom, then fill `planning/spot_check_queries.csv`, run `onestream_spot_check.ipynb` once with `RUN_LABEL = "before"` and once with `RUN_LABEL = "after"`, and finally send back the generated `dify_exports/` CSV and JSON files plus `assets/figures/`. Before Linus runs real data, the acceptance items that deserve the closest look are the real KB folder pointers, availability of Sentence-BERT, FAISS, gensim, and OpenAI on the Maersk machine, the final `planning/spot_check_queries.csv` contents, and whether the optional cross-encoder and LLM calls should be enabled for the live run.
